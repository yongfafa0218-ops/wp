# 🍯 첫 수익까지 로드맵 — "워드프레스에 붙이는 법 + 구글 세팅 완전 정복"

> 대상: 도구(HoneyPot)는 다뤄봤는데 **워드프레스·서치콘솔·애드센스가 처음인 분**
> 목표: "변환한 글을 언제, 어떻게, 어디에 붙이는지"가 머릿속에 그림으로 그려지는 것

---

## 🗺️ 30초 요약 — 전체 그림

```
[지금 하는 일]        [글 5~10개 쯤]        [글 15~20개 쯤]        [승인 후]
도메인+호스팅+WP 설치 → 글 쓰고 발행(수동/자동) → 서치콘솔 등록      → 광고 코드 넣고 수익 시작
        ↓                    ↓                      ↓                    ↓
   설정 탭 채우기       툴의 ③ 탭 사용            사이트맵 제출        애드센스 승인 (3일~2주)
```

**가장 중요한 시점 3개만 기억하세요:**

| 시점 | 무엇을 | 왜 |
|---|---|---|
| **글 쓰기 전 (1회)** | ⚙️ 설정 탭에 WP 주소·아이디·앱 패스워드 입력 | ③ 탭의 "🚀 자동 발행" 버튼이 동작하게 됨 |
| **글 5~10개 후** | 구글 서치콘솔 등록 + 사이트맵 제출 | 구글이 내 글을 색인(인식)하기 시작 |
| **글 15~20개 + 필수 페이지 후** | 애드센스 신청 | 승인되면 광고 코드를 넣을 수 있음 |

---

## Phase 0 — 아직 워드프레스가 없다면 (하루)

> 이미 사이트가 있으면 이 장은 건너뛰세요.

### 1. 도메인 + 호스팅 고르기 (2026년 기준)

| 선택지 | 비용 | 추천 대상 |
|---|---|---|
| **카페24 매니지드 워드프레스** | 월 450원~ (자동 설치·백업·SSL 포함) | 🇰🇷 초보자 1순위 — 한글 UI, 워드프레스 자동 설치됨 |
| Hostinger | 월 ~1,000원대 (도메인 1년 포함 이벤트 많음) | 저렴하게 글로벌 속도 원할 때 |
| 닷홈(Dothome) | 무료~ | 연습용. 단, 애드센스는 독자 도메인·안정성 때문에 유료 권장 |
| Cloudways | 월 1만원대~ | 나중에 사이트가 커지면 갈아탈 곳 |

- 도메인(주소)은 **`myblog.com` 같은 독자 도메인**이어야 합니다. 티스토리/무료 서브도메인은 애드센스 심사에서 불리해요.
- 카페24 매니지드 워드프레스는 가입 → 결제하면 **워드프레스가 이미 설치된 상태**로 시작합니다. 별도 설치 과정 없음.

### 2. 설치 후 확인할 것
- [ ] `https://내주소.com/wp-admin` 에 로그인되는가 (WP 관리자)
- [ ] 주소가 `https://`로 시작하는가 (SSL) — 카페24는 자동 적용
- [ ] 관리자 비밀번호를 별도 메모해뒀는가

---

## Phase 1 — 워드프레스 기본 세팅 (30분)

### ① 필수 플러그인 3개만 설치 (플러그인 → 새로 추가 → 검색 → 지금 설치 → 활성화)

| 플러그인 | 용도 | 이유 |
|---|---|---|
| **Rank Math** (또는 Yoast SEO) | SEO 설정 | 제목/설명 입력창, 사이트맵 자동 생성, 스키마 관리 |
| **Site Kit by Google** | 구글 공식 | 서치콘솔·애드센스 연결을 한 번에 (나중에 활용) |
| **Akismet** 또는 **Antispam** | 스팸 방지 | 댓글 스팸 차단 (기본 내장도 OK) |

> ⚠️ 광고 플러그인, 슬라이더, 페이지 빌더 등은 **지금 깔지 마세요.** 많을수록 느려지고 애드센스 승인에 방해됩니다.

### ② 테마 (겉모양)
- 외모 → 테마 → 새로 추가 → **GeneratePress** 또는 **Kadence** 검색 → 설치·활성화
- 이유: 가볍고(빠름) 구글·애드센스가 좋아하는 구조

### ③ 카테고리 2~4개 만들기
- 글 → 카테고리 → 예: `금융`, `IT`, `정부지원` 등
- 애드센스 심사가 "사이트가 제대로 운영 중인가"를 볼 때 카테고리가 잡혀 있으면 유리합니다.

---

## Phase 2 — 툴의 ⚙️ 설정 탭 채우기 (5분, 글 쓰기 전 1회)

각 칸이 "언제 필요한지"만 알면 됩니다:

| 입력칸 | 무엇인가 | 언제/왜 필요한가 |
|---|---|---|
| 프록시 주소 | `http://localhost:8787` | **지금.** 키워드 분석(①)·자동 발행(③)에 필요. `python proxy.py` 실행 후 새로고침 |
| 내부 링크 목록 | 내 블로그 글 목록 `제목 \| https://…` | 글 쓸 때마다 자동 반영. 처음엔 비워도 됨 |
| 광고 코드 | 애드센스 광고 HTML | **애드센스 승인 후에만!** 지금은 빈칸 |
| 사이트 주소 | `https://myblog.com` | ③ 탭 자동 발행용 |
| 사용자명 | WP 로그인 아이디 | 〃 |
| 애플리케이션 패스워드 | WP가 발급해주는 24자 코드 | 〃 |

### 애플리케이션 패스워드 만드는 법 (딱 1번)
1. WP 관리자 → **사용자 → 프로필** → 맨 아래 "애플리케이션 비밀번호"
2. 이름에 `honeypot` 입력 → **새 애플리케이션 비밀번호 추가**
3. 나온 **24자 코드(공백 포함)** 복사 → 툴 설정의 "애플리케이션 패스워드"에 붙여넣기
4. 툴에서 **💾 설정 저장** → **🔌 WP 연결 테스트** → "✅ 연결 성공" 확인

> ⚠️ 코드는 이때만 보입니다. 잃어버리면 지우고 다시 만들면 됩니다.

---

## Phase 2.5 — (선택) 로컬 워드프레스로 먼저 테스트해보기 ⭐

> **호스팅을 사기 전에**, 지금 데스크톱에 깔린 로컬 WP로 "🚀 자동 발행"이 실제로 되는지 미리 확인할 수 있습니다.
> 로컬 WP도 REST API가 그대로 있어서, 같은 로직으로 글이 만들어집니다.

### ✅ 로컬에서 되는 것 / ❌ 안 되는 것

| 되는 것 | 안 되는 것 (호스팅 후 해야 함) |
|---|---|
| 🚀 자동 발행 (글 생성·예약) | 애드센스 신청 (실서버 도메인 필요) |
| 🔌 연결 테스트 | 구글 서치콘솔 색인 (인터넷 주소 필요) |
| HTML·스키마·SEO 점검 (브라우저라서 그냥 됨) | 실제 검색 노출·수익 (당연히) |
| 키워드 분석 (구글 자동완성 — WP와 무관) | - |

### 1) 내 로컬 WP 주소 확인하기

설치 방식마다 주소가 다릅니다. **WP 관리자에 로그인했을 때 브라우저 주소창**을 보면 됩니다:

| 설치 방식 | WP 관리자 주소 예시 | 툴 설정에 넣을 값 |
|---|---|---|
| **Local by Flywheel** | `https://mysite.local/wp-admin/` | `https://mysite.local` |
| **XAMPP / AMPP** | `http://localhost/wordpress/wp-admin/` | `http://localhost/wordpress` |
| **Bitnami** | `http://localhost/wordpress/wp-admin/` | `http://localhost/wordpress` |
| **Docker (포트 지정)** | `http://localhost:8080/wp-admin/` | `http://localhost:8080` |

> 💡 관리자 화면에 로그인된 주소에서 `/wp-admin/` 부분을 자르면 됩니다.

### 2) 애플리케이션 패스워드 만들기 (Phase 2와 동일)
WP 관리자 → **사용자 → 프로필** → 맨 아래 "애플리케이션 비밀번호" → 이름 `honeypot` → 추가 → **24자 코드 복사**

### 3) 툴 ⚙️ 설정 탭에 입력 → 저장 → 🔌 연결 테스트
- 사이트 주소: 위 표에서 확인한 값
- 사용자명 / 애플리케이션 패스워드 입력
- **💾 설정 저장** → **🔌 WP 연결 테스트** → "✅ 연결 성공" 확인

### 4) 글 하나 발행해보기
- ② 프롬프트 생성 → AI 글 받기 → ③ 변환 → **🚀 워드프레스 예약 발행**
- 로컬 WP 관리자 → **글 → 모든 글** 에 글이 생성되어 있는지 확인!

### ⚠️ 로컬 테스트 주의사항 2가지
1. **자체서명 SSL (Local by Flywheel)**: `https://mysite.local`은 가짜 인증서라서 연결이 거부될 수 있는데, **proxy.py가 자동으로 감지해 인증서 검증 없이 재시도**합니다 (로그에 "자체서명 SSL 감지" 메시지). 그냥 시도하면 됩니다.
2. **로컬에서 만든 글은 인터넷에 공개되지 않습니다** — 다른 사람/구글이 볼 수 없어요. 확인 용도일 뿐이라는 점만 기억하세요.

### 5) 호스팅으로 옮길 때 바꿀 것
- ⚙️ 설정의 **사이트 주소**만 실도메인(예: `https://myblog.com`)으로 교체
- **새 애플리케이션 패스워드** 만들기 (앱 패스워드는 사이트마다 별도 — 로컬에서 만든 건 실서버에서 안 먹힘)
- 나머지(내부 링크·광고 코드·애드센스 ID)는 그대로 유지됩니다

---

## Phase 3 — 변환한 글을 워드프레스에 붙이기 (매일 하는 일)

역공급 변환(③ 탭)까지 끝났다면, 결과물을 붙이는 방법은 **2가지**입니다.

### 방법 A — 🚀 자동 발행 (추천, 10초)

Phase 2에서 연결만 해두면:

1. ③ 탭에서 변환 완료 → **예약 발행 일시** 선택 (비워두면 즉시 발행)
2. **🚀 워드프레스 예약 발행** 버튼 클릭
3. "✅ 발행/예약 성공!" 확인 → 게시글 보기 링크 클릭

> 이 버튼은 HTML·제목·슬러그·예약 시간을 전부 REST API로 알아서 넣어줍니다. **매일 쓰게 될 버튼이 바로 이것.**

### 방법 B — 수동 붙여넣기 (자동이 안 될 때)

1. WP 관리자 → **글 → 새 글 추가**
2. 제목 입력
3. 우측 상단 **⋮ (점 3개)** → **코드 편집기** 선택
4. 툴의 **`</> HTML` 탭** → 전체 복사 → 코드 편집기에 붙여넣기
5. 다시 **⋮ → 블록 편집기**로 복귀 → 우측 **미리보기**로 모양 확인
6. 우측 **발행** → **예약** → 날짜·시간 → **예약** (또는 즉시 발행)

### 발행 전 10초 체크 (툴이 이미 해줌)
- ③ 탭의 **🔍 구글 SEO 점검**이 65점 이상인가 → 80점 이상이면 발행 OK
- 메타 제목 60자 이하, 설명 155자 이하 (경고가 뜨면 줄이기)
- 글 하나당 이 흐름이 **15분 안에** 끝납니다: 키워드 선택 → 프롬프트 → AI → 변환 → 발행

---

## Phase 4 — 구글 서치콘솔 등록 (글 5~10개 쯤, 20분)

> "구글이 내 사이트를 알고 있나"를 확인·요청하는 창구. **등록만 해두면 이후 글은 자동으로 수집됩니다.**

### 등록 절차
1. **search.google.com/search-console** 접속 (구글 계정)
2. **"URL 접두어"** 방식 선택 → `https://myblog.com` 입력
3. 소유권 확인 — 가장 쉬운 방법: **Site Kit 플러그인** (Phase 1에서 설치한 것)
   - Site Kit → "사이트를 Search Console에 연결" → 구글 계정 승인 → 끝
   - (Site Kit가 싫으면: Rank Math → 일반 설정 → 웹마스터 도구 → 구글 검증 코드 붙여넣기)
4. 좌측 메뉴 **색인 생성 → 사이트맵**

### 사이트맵 제출 — 뭘 넣어야 하나?
- **Rank Math/Yoast 사용 중** → 그냥 `sitemap_index.xml` 입력 → 제출. (플러그인이 알아서 만듦)
- **플러그인 없음** → 툴 ③ 탭의 **🔍 SEO 점검**에서 `🦴 sitemap.xml` 복사 → 카페24 파일매니저로 서버 루트(public_html)에 `sitemap.xml`로 업로드 → `sitemap.xml` 제출
- 📌 사실상 "사이트맵"은 WP 주소만 정확하면 됩니다. 너무 어렵게 생각하지 마세요.

### 이후 습관 (선택)
- 글 발행 후 GSC → **URL 검사** → 발행한 글 주소 붙여넣기 → **색인 생성 요청** — 초기 2~4주만 이렇게 해주면 이후엔 자동.

---

## Phase 5 — 애드센스 신청 (글 15~20개 쯤)

### 신청 전 체크리스트 (2026년 기준 — 하나라도 빠지면 심사가 자동으로 중단됨)
- [ ] **독자 도메인**(https://) + SSL 적용
- [ ] 글 **15~20개 이상** (검수된 퀄리티, 카테고리별로 분산)
- [ ] **소개(About)** 페이지 — 운영자 신원·사이트 목적
- [ ] **개인정보처리방침(Privacy Policy)** — "쿠키·광고 데이터 수집" 고지 포함 (애드센스 사용 예정 명시)
- [ ] **문의(Contact)** 페이지 — 실제 동작하는 이메일/양식
- [ ] 사이트가 **2~4주 이상 운영**된 느낌 (등록일 기준이 아니라 "진짜 운영 중"처럼 보이는 것)
- [ ] 모바일에서 깨지지 않는 테마 (GeneratePress/Kadence면 OK)

### 신청 절차
1. **adsense.google.com** 접속 → 시작 → 사이트 주소 입력 → 계정 정보 작성
2. 심사: **보통 3일~2주** (콘텐츠 30편+면 1~3일, 20편이면 3~7일)
3. 결과는 메일 + 콘솔에서 확인

### 거절되면? (처음에 흔함 — 좌절 금지)
- 메일에 적힌 사유 확인 → 대부분 "콘텐츠 부족" → **2주 동안 글 더 쓰고** (10편+) → 필수 페이지 점검 → **재신청**
- 절대 다른 계정으로 우회 신청하지 마세요. 평생 블랙리스트입니다.

### 승인 후 — 드디어 광고!
1. 툴 **④ 구글 세팅** 탭 → 애드센스 발행자 ID(`ca-pub-…`) 입력 → **헤더 코드 복사**
2. WP에 넣는 가장 안전한 방법: 플러그인 **"Insert Headers and Footers"** 설치 → Headers에 붙여넣기
3. 애드센스 콘솔 → **광고 → 사이트 전체 광고(자동 광고)** 켜기 → 전면·인피드·인아티클 자동 배치
4. (선택) ⚙️ 설정 탭의 광고 코드에 인아티클 광고 코드를 넣으면 ③ 변환 때 글 중간 포인트에도 삽입

---

## 📅 하루 루틴 예시 (발행 습관 만들기)

| 시간 | 할 일 |
|---|---|
| 30분 | 키워드 분석(①)으로 오늘 쓸 키워드 1~2개 고르기 |
| 30분 | 프롬프트 생성(②) → 무료 AI에 붙여넣기 → 글 받기 |
| 10분 | 역공급 변환(③) → SEO 점검 80점 이상 확인 → **검수** (숫자·요건 사실 확인!) |
| 1분 | 🚀 발행 (예약 가능) |

> ⚠️ **하루 1~2개, 반드시 검수 후 발행.** AI 글을 하루 10개씩 쏟아내면 구글 "스케일 콘텐츠 남용" 패널티로 사이트 전체가 죽을 수 있습니다.

---

## ❓ 자주 묻는 질문

**Q. 사이트맵/robots.txt 꼭 해야 하나요?**
A. Rank Math 같은 SEO 플러그인을 쓰면 자동이니 신경 쓸 필요 없습니다. 툴이 만들어주는 건 "플러그인 없이" 하는 경우용입니다.

**Q. 광고 코드 언제 넣나요?**
A. **애드센스 승인 후에만.** 승인 전에는 광고가 안 나오고, 코드만 사이트를 느리게 만듭니다.

**Q. 예약 발행은 뭐가 좋나요?**
A. 정해진 시간에 글이 올라가서 "꾸준히 운영하는 사이트"처럼 보입니다. 아침 8시 등 규칙적인 시간을 추천.

**Q. 글 검수는 뭘 봐야 하나요?**
A. ① 숫자·요건이 맞는지 (대출 금리, 지원금액 등은 공식 사이트로 확인) ② 문장이 어색하지 않은지 ③ 제목/첫문단에 키워드가 있는지. 이 3개면 충분.

**Q. 언제쯤 돈이 나오나요?**
A. 현실적으로: 색인(2~4주) → 노출 → 클릭 → 애드센스 승인(3일~2주)까지 **2~3개월** 잡으세요. 처음엔 월 1만원 이하여도 정상입니다. 키워드·RPM 데이터가 쌓이면(④ 탭의 현실 정리 참고) 점점 올라갑니다.

**Q. 로컬 WP로 테스트해도 되나요?**
A. 네! Phase 2.5 참고. 주소만 정확히 넣으면 자동 발행까지 전부 테스트 가능합니다. 단, 자체서명 SSL은 프록시가 자동 처리하고, 만든 글이 인터넷에 공개되진 않습니다.

---

## 📖 용어 사전 (모르면 여기서)

| 용어 | 뜻 |
|---|---|
| 호스팅 | 내 사이트 파일을 저장·서비스해주는 컴퓨터를 빌리는 것 |
| 도메인 | `myblog.com` 같은 인터넷 주소 |
| SSL/HTTPS | 주소창의 자물쇠. 구글이 보안 사이트를 선호·애드센스 필수 |
| REST API | 툴이 WP에 "이 글로 발행해줘"라고 말하는 공식 통로 (애플리케이션 패스워드로 열림) |
| 애플리케이션 패스워드 | WP 로그인 비밀번호 대신 기계(툴) 전용으로 만든 비밀번호 |
| 사이트맵 | "내 사이트에 이런 글들이 있어요" 목록 — 구글 수집을 도움 |
| 색인(Indexing) | 구글이 내 글을 검색 결과에 등록하는 것 |
| 스키마(JSON-LD) | 글에 "이건 FAQ야"라고 구글에게 구조적으로 설명하는 주석 |
| E-E-A-T | 전문성·경험·권위·신뢰 — 구글의 콘텐츠 평가 기준 |
| RPM | 조회 1,000회당 수익. 애드센스 실적의 핵심 지표 |
| Vignette(전면 광고) | 모바일에서 화면 전환 시 뜨는 풀스크린 광고 |
| 인피드 광고 | 목록(피드) 사이에 끼는 광고 |

---

## ⚠️ 하지 마세요 5가지

1. **무료 호스팅/무료 도메인으로 애드센스 신청** → 심사에서 거의 탈락
2. **AI 글 무검수 대량 발행** → 사이트 전체 패널티
3. **애드센스 승인 전 광고 코드 삽입** → 아무 의미 없음
4. **스키마를 Yoast/RankMath와 이중으로 넣기** → 오류 발생. 플러그인 쓰면 플러그인 것만
5. **거절 후 바로 재신청** → 2주 보강 후 재신청이 정석

---

*이 가이드는 2026년 8월 기준이며, 호스팅 요금·애드센스 심사 기준은 변경될 수 있습니다.*
